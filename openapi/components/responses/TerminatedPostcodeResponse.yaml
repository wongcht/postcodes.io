title: Terminated Postcode Response
type: object
required:
  - status
  - result
properties:
  status:
    type: integer
    format: int32
    enum:
      - 200
  result:
    $ref: "../schemas/TerminatedPostcode.yaml"
    description: "Data for the requested terminated postcode."
